---
// HeroSection.astro
---

<section
  id="inicio"
  class="relative min-h-screen flex items-center justify-center pt-20"
>
  <!-- Gradient orbs -->
  <div
    class="absolute top-1/4 left-1/4 w-96 h-96 bg-primary/20 rounded-full blur-[120px] animate-pulse"
  >
  </div>
  <div
    class="absolute bottom-1/4 right-1/4 w-80 h-80 bg-accent/20 rounded-full blur-[100px] animate-pulse animation-delay-1000"
  >
  </div>

  <div
    class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center"
  >
    <div class="max-w-4xl mx-auto hero-content">
      <!-- Main heading -->
      <h1
        class="font-heading text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold leading-tight mb-6"
      >
        <span class="text-white">De una idea simple a una </span><span
          class="bg-clip-text text-[#00D6A1] bg-linear-to-r from-[00D6A1]/10 to-[00D6A1]/80"
          >creación increíble</span
        >
      </h1>

      <!-- Subtitle -->
      <p
        class="text-gray-400 text-lg sm:text-xl text-muted-foreground max-w-2xl mx-auto mb-10"
      >
        Automatiza procesos, crea experiencias web excepcionales y transforma tu
        negocio con soluciones de IA a medida.
      </p>

      <!-- CTA Buttons -->
      <div
        class="flex flex-col sm:flex-row items-center justify-center gap-4 cta-buttons "
      >
        <button
          class="bg-[#00D6A1] hover:bg-[#00D6A1]/90 text-primary-foreground font-semibold px-8 py-6 text-lg rounded-md transition-all group inline-flex items-center shadow-[0_0_40px_hsl(165_100%_42%/0.3)]"
        >
          Comenzar Proyecto
          <svg
            class="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
          </svg>
        </button>
        <button
          class="text-amber-50 border-2 border-border hover:bg-[#ed1d85] hover:shadow-[#ed1d85] hover:text-primary px-8 py-6 text-lg rounded-md transition-colors bg-background"
        >
          Ver Nuestro Trabajo
        </button>
      </div>
    </div>
  </div>

</section>

<style>
  /* Animation delay utility */
  .animation-delay-1000 {
    animation-delay: 1s;
  }

  /* Fade in animations */
  .hero-content {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .badge-animate {
    animation: fadeInScale 0.6s ease-out 0.2s forwards;
    opacity: 0;
  }

  .cta-buttons {
    animation: fadeInUp 0.6s ease-out 0.4s forwards;
    opacity: 0;
  }

  .stats-grid {
    animation: fadeIn 0.6s ease-out 0.6s forwards;
    opacity: 0;
  }

  .scroll-indicator {
    animation: fadeIn 0.6s ease-out 1s forwards;
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes fadeInScale {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  /* Scroll indicator animation */
  .scroll-dot {
    animation: scrollBounce 1.5s ease-in-out infinite;
  }

  @keyframes scrollBounce {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(8px);
    }
  }

  /* Stat items fade in */
  .stat-item {
    animation: fadeIn 0.6s ease-out forwards;
    opacity: 0;
  }
</style>